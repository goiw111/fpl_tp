{
  "name": "api",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.10.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --local --live-reload",
    "deploy": "wrangler publish",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^3.0.2"
  }
}
